{
  "name": "ItemValueSnapshot",
  "type": "object",
  "description": "A point-in-time valuation snapshot for any collectible item. Generalizes BottleValueSnapshot to support all modules (pipekeeper, whiskeykeeper, cigarkeeper, winekeeper). Works for pipes, tobacco blends, bottles, cigars, and wine.",
  "properties": {
    "module_key": {
      "type": "string",
      "enum": ["whiskeykeeper", "pipekeeper", "cigarkeeper", "winekeeper", "other"],
      "description": "Module this snapshot belongs to (e.g. pipekeeper, whiskeykeeper)"
    },
    "item_id": {
      "type": "string",
      "description": "ID of the item (pipe, tobacco blend, bottle, etc.) this snapshot belongs to"
    },
    "created_by": {
      "type": "string",
      "description": "Email of the user who created this snapshot"
    },
    "snapshot_date": {
      "type": "string",
      "format": "date",
      "description": "Date of the valuation snapshot (YYYY-MM-DD)"
    },
    "computed_value": {
      "type": "number",
      "description": "Engine-computed canonical current value at time of snapshot"
    },
    "source": {
      "type": "string",
      "description": "What data source drove the primary value (e.g. Manual Market Value, Price Per Oz, Collector Value)"
    },
    "confidence": {
      "type": "string",
      "enum": ["high", "medium", "low"],
      "description": "Confidence level of the computed value"
    },
    "rarity_score": {
      "type": "number",
      "description": "Engine-computed rarity score 0–100 at time of snapshot"
    },
    "replacement_difficulty": {
      "type": "string",
      "enum": ["easy", "moderate", "hard", "very_hard"],
      "description": "Estimated difficulty of replacing this item at time of snapshot"
    },
    "recommendation": {
      "type": "string",
      "description": "Strategy recommendation at time of snapshot (e.g. use, rotate, preserve, insure for pipes; smoke_now, smoke_later, cellar, hold_for_trade for tobacco)"
    },
    "notes": {
      "type": "string",
      "description": "Optional free-text notes about this valuation"
    }
  },
  "required": ["module_key", "item_id", "created_by", "snapshot_date"]
}
