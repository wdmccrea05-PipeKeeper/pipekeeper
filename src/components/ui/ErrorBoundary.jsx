/**
 * Thin re-export — canonical ErrorBoundary lives at @/components/system/ErrorBoundary.
 * All new imports must use @/components/system/ErrorBoundary directly.
 */
export { default } from "@/components/system/ErrorBoundary";
